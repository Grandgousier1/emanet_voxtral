# This file contains the Python dependencies for the GPU environment.
# It is recommended to install these in a virtual environment.

# Core ML/DL framework
torch>=2.0.0

# Hugging Face libraries for models and processing
# Voxtral requires transformers >= 4.54.0
transformers>=4.54.0
# Voxtral requires mistral-common with audio support
mistral-common[audio]>=1.8.1

# ASR / Transcription libraries
# Used in the "two_step" pipeline
whisperspeech
# Used as a fallback ASR model
faster-whisper

# YouTube downloader
yt-dlp

# Configuration file parsing
PyYAML

# Progress bars
tqdm

# For audio processing
torchaudio
